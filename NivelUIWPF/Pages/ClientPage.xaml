<Page x:Class="NivelUIWPF.Pages.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Clienți" Background="#ECF0F1">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Top" Spacing="15">
            <TextBlock Text="INFORMAȚII CLIENȚI" 
                       FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                <StackPanel Spacing="10">
                    <TextBlock Text="Adăugare / Editare Client" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                    
                    <StackPanel>
                        <TextBlock Text="ID:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="IdTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Nume:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="NumeTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Telefon:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="TelefonTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Email:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="EmailTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Adăugare" Click="AddClient" Style="{StaticResource ModernButtonStyle}" Width="150"/>
                        <Button Content="Ștergere" Click="DeleteClient" Style="{StaticResource AccentButtonStyle}" Width="150"/>
                        <Button Content="Refresh" Click="RefreshClientList" Style="{StaticResource ModernButtonStyle}" Width="150"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Lista Clienți" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                    
                    <DataGrid x:Name="ClientDataGrid" AutoGenerateColumns="False" Height="300" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Nume" Binding="{Binding Nume}" Width="150"/>
                            <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="130"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="3" Padding="15" Background="#F8F9F9">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Detalii Client Selectat" FontSize="13" FontWeight="Bold" Foreground="#3498DB"/>
                            <TextBlock x:Name="SelectedInfoBlock" Text="Selectați un client din listă pentru a vedea detaliile" Foreground="#555555"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
