<Page x:Class="NivelUIWPF.Pages.ProdusLemnPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Produs Lemn" Background="#ECF0F1">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Top" Spacing="15">
            <TextBlock Text="INFORMAȚII PRODUS LEMN" 
                       FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                <StackPanel Spacing="10">
                    <TextBlock Text="Adăugare / Editare Produs Lemn" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                    
                    <StackPanel>
                        <TextBlock Text="ID:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="IdTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Tip Produs:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="TipProdusTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Cantitate (ton):" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="CantitateTextBox" Style="{StaticResource ModernTextBoxStyle}" Text=""/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Caracteristici:" Foreground="#2C3E50" FontWeight="Bold" Margin="0,0,0,3"/>
                        <CheckBox x:Name="UscatCheck" Content="Uscat" Margin="0,5,0,0"/>
                        <CheckBox x:Name="TratatCheck" Content="Tratat"/>
                        <CheckBox x:Name="LustruitCheck" Content="Lustruit"/>
                        <CheckBox x:Name="IgnifugatCheck" Content="Ignifugat"/>
                        <CheckBox x:Name="CertificatCheck" Content="Certificat"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Adăugare" Click="AddProdusLemn" Style="{StaticResource ModernButtonStyle}" Width="150"/>
                        <Button Content="Ștergere" Click="DeleteProdusLemn" Style="{StaticResource AccentButtonStyle}" Width="150"/>
                        <Button Content="Refresh" Click="RefreshProdusLemnList" Style="{StaticResource ModernButtonStyle}" Width="150"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Lista Produse Lemn" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                    
                    <DataGrid x:Name="ProdusLemnDataGrid" AutoGenerateColumns="False" Height="300" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                            <DataGridTextColumn Header="Tip Produs" Binding="{Binding TipProdus}" Width="120"/>
                            <DataGridTextColumn Header="Cantitate" Binding="{Binding Cantitate}" Width="100"/>
                            <DataGridTextColumn Header="Caracteristici" Binding="{Binding Caracteristici}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="3" Padding="15" Background="#F8F9F9">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Detalii Articol Selectat" FontSize="13" FontWeight="Bold" Foreground="#3498DB"/>
                            <TextBlock x:Name="SelectedInfoBlock" Text="Selectați un articol din listă pentru a vedea detaliile" Foreground="#555555"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
